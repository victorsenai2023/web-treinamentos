const input = require('readline-sync')

var nome = (input.question('Informe seu nome: '))
var idade = parseInt(input.question('Informe sua idade: '))
var resultado

switch (idade) {
    case '1':
        resultado =  `
        break;
   

    default:
        resultado = `Nenhuma opção selecionada para converter ${valor.toFixed(2)}`
        break;
}

console.log(resultado)